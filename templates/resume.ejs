<!-- resume.ejs -->
<!DOCTYPE html>
<html>
<head>
    <title>Resume</title>
</head>
<body>
    <h1>Professional Summary</h1>
    <p><%= professionalSummary %></p>
    
    <h2>Skills</h2>
    <ul>
        <% skillList.forEach(skill => { %>
            <li><%= skill %></li>
        <% }); %>
    </ul>
</body>
</html>
